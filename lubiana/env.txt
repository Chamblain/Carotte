sudo apt update
sudo apt install -y python3 python3-venv python3-pip
sudo apt install -y pcscd pcsc-tools libpcsclite1 libpcsclite-dev
sudo systemctl enable --now pcscd

python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install pyscard mysql-connector-python flask

